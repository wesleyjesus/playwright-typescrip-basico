# Legacy tests

Os cenários antigos foram substituídos por uma estrutura baseada em Page Objects, AAA e helpers reutilizáveis.

Use as suítes organizadas em `tests/login/` e `tests/documento/` como referência principal.
